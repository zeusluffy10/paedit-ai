# Generated by GitHub Copilot
services:
  - type: web
    name: photo-motion-ai-backend
    runtime: python
    plan: starter
    rootDir: src/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    envVars:
      - key: APP_ENV
        value: production
      - key: STORAGE_DIR
        value: /tmp/photo-motion-ai-data
      - key: MOCK_PROVIDER_DELAY_SECONDS
        value: "2"
      - key: CORS_ORIGINS
        value: https://your-mobile-web-domain.example
